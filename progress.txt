# Titrate — Progress Checkpoint

## Date: 2026-04-18 (late evening, post-brand-migrations session)

## Current state

- **Branch:** `master` · HEAD `6e6bbce` (Merge PR #12: step-flow AppShell + TimelineRail).
- **Open PRs:** none. PRs #5–#12 all merged to master in a single session.
- **Working tree:** clean (only `.remember/tmp/save-session.pid` shows as modified, as always).
- **CI:** `.github/workflows/ci.yml` green on master across the full PR chain.
- **Regression:** 1,406 tests across the workspace (unchanged from prior checkpoint — web 665, SDK 541 + 51 skipped, etc.).
- **Policy in effect:** small focused brand/theme migration PRs merge direct-to-master via quick PR → CI green → squash-merge. `feedback_direct-to-master.md` memory captures scope.

## Shipped on master this session

All eight PRs follow the same cut-branch → migrate → test → PR → CI → squash-merge rhythm.

### Page migrations (PRs #5, #6, #7)

- **PR #5 `f8d79ec` — HomePage chrome.** Wrap campaigns dashboard in `ModeProvider[brutalist]`; swap New Campaign button + archived toggle for `Button` primitives; brand h1 typography. CampaignCard + hover icon actions stayed — they migrated later in PR #8.
- **PR #6 `7ac14e1` — SettingsPage.** Wrap in `ModeProvider[brutalist]`; swap six form inputs for `Input` primitive (`(optional)` moves to `hint` prop); wrap config form in `Card`; replace Add Chain / Save / Cancel / Delete / Export Campaigns with `Button` primitives; config-list typography moved to brand tokens (`fg-primary`/`fg-muted`/`edge`). Import Campaigns retains `<label>`-wrapping-hidden-input pattern but adopts brutalist tokens. ChainSelector + EncryptedField + lock-warning banner preserved as-is. All 6 existing tests continue to pass.
- **PR #7 `265d1c5` — CampaignPage mode-aware step host.** CampaignPage is a thin step orchestrator — real migration lives in per-step components + legacy step-flow AppShell. This PR wraps the active step in `<ModeProvider mode={stepMode}>` keyed on `activeStepId`: distribute = operator, all other steps = brutalist. Step components can now branch via `useMode()` as they migrate without each one needing its own provider. Plus: fallback text (`No campaign selected`, `Loading campaign…`) moved to brand tokens.

### Component migrations (PRs #8, #9, #10, #11, #12)

- **PR #8 `cb0c1c0` — CampaignCard.** Swap ad-hoc ring/bg chrome for `Card` primitive (brutalist shadow + edge border). Retype name (font-sans extrabold) + meta row (font-mono fg-muted) + progress label (uppercase mono). Progress track: `edge/40` outer, `pink-500` inner. Brutalist hover press (1px translate + shadow reduction). Stabilize 3 progress-bar tests via `data-progress-bar` attribute (was brittle on `.bg-blue-500` class).
- **PR #9 `7c6f88b` — WelcomeCard.** Wrap empty-state in `ModeProvider[brutalist]`. Replace StepCards' chrome with `Card` primitive. Retype title/subtitle/step titles/descriptions with brand tokens. Swap blue-circle numbered badges for mono `01`/`02`/`03`/`04` labels in pink-600 (matches LandingPage "How it works" aesthetic). CTA → `Button variant="primary" size="lg"`. All 4 tests preserved.
- **PR #10 `5a03362` — StatusBadge.** Mode-aware now: brutalist renders filled chip-green/yellow/pink pills with hard 2px edge; operator renders tinted pills with `--color-ok`/`--color-err`/`--color-info` at 10% alpha. Five statuses (pending/active/complete/error/locked) map to semantic tones in both modes. Publishes `data-status` attribute — tests swap from `className.toContain('green')`-style assertions (the color literals no longer exist in the source). Consumers (CampaignCard, BatchStatusCard) pick up new styling automatically — external API unchanged.
- **PR #11 `861f9ac` — WalletBadge.** Header-embedded widget, visible on every non-landing page. Shell: `bg-card` + `edge/40` border (cascades via `[data-theme]` for light/dark). Address: `font-mono` at `fg-primary`. Chain: uppercase mono caption at `fg-muted`. Connected dot: `--color-ok`. Perry-mode indicator retoned from purple (not in brand palette) to `--color-warn`. Mode-agnostic — Header has no `ModeProvider` wrapping it, and the chosen tokens resolve via theme cascade rather than mode cascade.
- **PR #12 `6e6bbce` — Step-flow AppShell + TimelineRail.** Legacy step-flow chrome for CampaignPage's sidebar + mobile progress bar. Rail dots adopt the brand's titration-point motif: complete = filled pink-500, active = hollow pink-500 ring with 20%-alpha halo (mirrors brand mark's equivalence-point circle), locked = hollow edge/40 outline. Connector lines: `edge/30`. Step labels: `font-mono` at `fg-primary` with `pink-600` hover. Focus ring uses `--color-info`. Mobile segmented bar: `pink-500` / `pink-500 @60%` / `edge/20`. `data-status` attribute on each dot for stable test selectors.

## Remaining work

### Component migrations (long tail, still direct-to-master)

Authorized-and-policy-approved per `feedback_direct-to-master.md`. Each is its own small PR. Batch further PRs the same way.

- **Step components (7 files)** under `packages/web/src/steps/`: `CampaignStep`, `AddressesStep`, `FiltersStep`, `AmountsStep`, `WalletStep`, `RequirementsStep`, `DistributeStep`. These are the meat of the campaign flow. Thanks to PR #7, each step already sits inside a correctly-moded `ModeProvider` — consumers just need to use `useMode()` + brand primitives. `DistributeStep` is the only one in operator mode.
- **BatchStatusCard** (uses StatusBadge already migrated; also imports from legacy styling).
- **BatchTimeline** (step-like vertical timeline, observer-leaning — likely operator).
- **AddressTable** (table — consider `DataTable` primitive).
- **PipelineStepEditor** (decision-leaning — brutalist).
- **InterventionModal** (modal chrome — whole modal pattern could get brutalist treatment).
- **EncryptedField** (uses own micro-chrome; small touch).
- **ChainSelector** (presets grid — `Chip`-friendly).
- **Header** (`components/Header.tsx` — still legacy gray-200/gray-800 chrome; nav bar migration is a good single-PR target).
- **CampaignCardSkeleton** (match CampaignCard's new chrome).
- **ErrorBoundary**, **ReloadPrompt**, **ToastProvider** renderers (small polish).
- ~40 more scattered: `InlineEdit`, `Skeleton`, `TokenSelector`, `AmountInput`, various step-internal widgets.

### Phase 2 follow-on (separate from brand work)

- **Phase 2c** — TUI `PipelineOrchestrator` wired into the React tree, LIVE dashboard panel, keyboard bindings (space / s / f / d / g / S / e / b / q), viewer-mode for second session.
- **Phase 2d** — Web GUI orchestrator parity (same loop abstractions, Web Locks API instead of `.pipeline.lock`).
- **Phase 2e** — Anvil-gated e2e + error-surfacing polish.

## Session notes

- Pattern holds: push branch → `gh pr create` → `gh run watch` → `gh pr merge --squash --delete-branch`. `gh pr merge` post-merge pull fails on the session-pid dirty file every time; stash / pull / drop is the stock recipe. Reproducible enough it could be scripted.
- One transient vitest flake during PR #12 (ran 96 s vs normal 12 s — resource contention). Clean on rerun. Kept a note in the PR body; not worth gating work on.
- CI annotations still warn about Node 20 actions being deprecated Sep 2026. Not urgent.
- Eight PRs merged in one session with consistent discipline: every PR has a test plan, typecheck, regression, build. None of the 665 web tests broke. The pattern that made this possible was normalizing brittle class-string assertions to `data-*` attributes in two places (`CampaignCard`, `StatusBadge`) — future token changes won't need test updates.

## What to do next session

Pick any bullet from "Component migrations (long tail)" and follow the same rhythm. Suggested order:
1. **Step components** — biggest visual surface, `ModeProvider` already wired per-step via PR #7.
2. **Header migration** — same everywhere, visible on every page.
3. **BatchStatusCard / BatchTimeline / AddressTable** — distribution-view polish.
4. The rest as they come up in consuming pages.
